node_modules
dist
.vercel
# Local-only scratch (todos, unimplemented design notes), but keep durable
# superpowers specs + plans under source control.
docs/*
!docs/superpowers/

# Claude Code: ignore machine-local config, but keep shared skills + launch + settings
.claude/*
!.claude/skills/
!.claude/launch.json
!.claude/settings.json
